{
  "version": "1.0",
  "defaultSchema": "fedregister",
  "schemas": [{
    "name": "fedregister",
    "type": "custom",
    "factory": "org.apache.calcite.adapter.govdata.GovDataSchemaFactory",
    "operand": {
      "dataSource": "fedregister",
      "startYear": 2010,
      "autoDownload": true,
      "directory": "${GOVDATA_PARQUET_DIR}",
      "cacheDirectory": "${GOVDATA_CACHE_DIR}",
      "trackerBackend": "pg",
      "trackerConfig": {
        "jdbcUrl": "${GOVDATA_TRACKER_PG_URL}",
        "user": "${GOVDATA_TRACKER_PG_USER}",
        "password": "${GOVDATA_TRACKER_PG_PASSWORD}"
      },
      "s3Config": {
        "accessKeyId": "${AWS_ACCESS_KEY_ID}",
        "secretAccessKey": "${AWS_SECRET_ACCESS_KEY}",
        "endpoint": "${AWS_ENDPOINT_OVERRIDE}"
      }
    }
  }]
}
